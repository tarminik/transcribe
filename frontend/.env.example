# Where frontend should send API requests during development.
# Leave as /api when using Vite proxy (default).
VITE_API_BASE=/api

# Backend origin used to detect presigned upload/download URLs.
VITE_BACKEND_ORIGIN=http://localhost:8000
